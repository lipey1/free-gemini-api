# Obrigatório — chave para assinar JWTs de sessão (gere uma string longa e aleatória)
SESSION_SECRET=sua-chave-secreta-aqui

# Porta do servidor (padrão: 3333)
PORT=3333

# Se true, envolve cada prompt do usuário com src/config/default_prompt.txt (${message})
INSERT_PROMPT=false

# Tempo de vida do sessionToken em minutos (padrão: 45)
SESSION_TTL_MINUTES=45

# Caminho do SQLite de sessões; vazio usa data/sessions.sqlite
SESSION_DB_PATH=

# Rate limits (0 = desativado para aquele endpoint)
RATE_LIMIT_CREATE_SESSION_INTERVAL_SEC=15
RATE_LIMIT_CHAT_PER_MINUTE=30
RATE_LIMIT_STATUS_PER_MINUTE=60
